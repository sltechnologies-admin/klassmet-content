[
  {
    "id": "ex_heap_sort",
    "title": "Heap Sort – In-place O(n log n)",
    "description": "Java heap sort using heapify and in-place max-heap extraction.",
    "language": "java",
    "code": "class HeapSort {\n    static void heapify(int[] arr, int n, int i) {\n        int largest = i, l = 2 * i + 1, r = 2 * i + 2;\n        if (l < n && arr[l] > arr[largest]) largest = l;\n        if (r < n && arr[r] > arr[largest]) largest = r;\n        if (largest != i) {\n            int tmp = arr[i]; arr[i] = arr[largest]; arr[largest] = tmp;\n            heapify(arr, n, largest);\n        }\n    }\n    static void heapSort(int[] arr) {\n        int n = arr.length;\n        for (int i = n / 2 - 1; i >= 0; i--) heapify(arr, n, i);\n        for (int i = n - 1; i > 0; i--) {\n            int tmp = arr[0]; arr[0] = arr[i]; arr[i] = tmp;\n            heapify(arr, i, 0);\n        }\n    }\n    public static void main(String[] args) {\n        int[] arr = {12, 11, 13, 5, 6, 7};\n        heapSort(arr);\n        for (int x : arr) System.out.print(x + \" \");\n    }\n}",
    "explanation": "Phase 1: BuildHeap O(n). Phase 2: n extractions each O(log n). Total O(n log n). Space O(1).",
    "order": 1
  }
]
