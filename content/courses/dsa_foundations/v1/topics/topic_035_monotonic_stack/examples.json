[
  {
    "id": "ex_monotonic_stack",
    "title": "Monotonic Stack – Next Greater Element",
    "description": "Find next greater element for every array position in O(n) using a monotonic stack.",
    "language": "java",
    "code": "import java.util.Stack;\nclass MonotonicStack {\n    static int[] nextGreater(int[] arr) {\n        int n = arr.length;\n        int[] result = new int[n];\n        Stack<Integer> st = new Stack<>();\n        for (int i = n - 1; i >= 0; i--) {\n            while (!st.isEmpty() && st.peek() <= arr[i]) st.pop();\n            result[i] = st.isEmpty() ? -1 : st.peek();\n            st.push(arr[i]);\n        }\n        return result;\n    }\n    public static void main(String[] args) {\n        int[] arr = {4, 5, 2, 10};\n        int[] res = nextGreater(arr);\n        for (int x : res) System.out.print(x + \" \");\n    }\n}",
    "explanation": "Traverse right to left. Maintain decreasing stack. Each element pushed and popped once. Time: O(n).",
    "order": 1
  }
]
