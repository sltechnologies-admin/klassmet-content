The two-pointer technique uses two indices moving through an array to solve problems more efficiently than brute force.

In this topic, you will see how two pointers — like two people walking toward each other through a tunnel — reduce time complexity from O(n²) to O(n).
