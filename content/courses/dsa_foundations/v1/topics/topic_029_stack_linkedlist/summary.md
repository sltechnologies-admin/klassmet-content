A stack can be implemented using a singly linked list where new elements are always inserted at the head (top).

In this topic you will implement a dynamic stack with no fixed size limitation using linked nodes.
