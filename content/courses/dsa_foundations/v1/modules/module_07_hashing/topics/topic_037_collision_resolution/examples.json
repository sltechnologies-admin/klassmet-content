[
  {
    "id": "ex_linear_probing",
    "title": "Collision Resolution – Linear Probing",
    "description": "Java hash table using linear probing for collision resolution.",
    "language": "java",
    "code": "class LinearProbingHash {\n    int[] keys; int size;\n    LinearProbingHash(int size) { this.size = size; keys = new int[size]; java.util.Arrays.fill(keys, -1); }\n    void insert(int key) {\n        int idx = key % size;\n        while (keys[idx] != -1) idx = (idx + 1) % size;\n        keys[idx] = key;\n    }\n    boolean search(int key) {\n        int idx = key % size;\n        while (keys[idx] != -1) {\n            if (keys[idx] == key) return true;\n            idx = (idx + 1) % size;\n        }\n        return false;\n    }\n}",
    "explanation": "When collision detected, probe next slot linearly. Wraps using modulus. Avg O(1), Worst O(n).",
    "order": 1
  }
]
