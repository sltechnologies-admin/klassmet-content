A hash table stores key-value pairs and uses a hash function to map each key to an index for near-constant-time lookups.

In this topic you will learn how a hash table is like a locker room — each locker number is calculated from your name so you can find your locker immediately.
