A binary tree is a hierarchical structure where each node has at most two children — left and right.

In this topic you will learn how trees describe hierarchies like an organisation chart — the CEO at the root, managers as internal nodes, and employees as leaves.
