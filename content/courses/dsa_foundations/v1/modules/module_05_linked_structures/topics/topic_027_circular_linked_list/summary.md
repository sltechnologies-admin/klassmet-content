A circular linked list is one where the tail node's next pointer points back to the head, forming a loop.

In this topic you will learn circular lists (like a round-robin scheduler), how to reverse a linked list, and how Floyd's cycle detection algorithm finds loops in O(1) space.
