An array is a collection of elements of the same data type stored in contiguous memory locations.

In this topic, you will learn how arrays work like seats in a cinema hall row — each seat has a fixed number (index) and you can jump directly to any seat.
