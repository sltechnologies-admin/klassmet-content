Prefix sum is a technique to precompute cumulative sums, enabling fast range queries in O(1) time.

In this topic, you will learn how prefix sum avoids recomputing sums repeatedly — like keeping a monthly running total of expenses to quickly find spending in any date range.
